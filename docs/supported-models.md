# Supported Models

PRIME-RL supports any HuggingFace model via the default `transformers` backend. For some architectures, we provide custom implementations with better performance.

<!-- TODO: add model compatibility table with columns: model, custom impl, EP support, SFT, RL -->
