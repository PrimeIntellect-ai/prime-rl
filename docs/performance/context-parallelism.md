# Context Parallelism

Context parallelism (CP) splits long sequences across GPUs, enabling training on sequences that exceed single-GPU memory. Enable it with `--model.cp`.

<!-- TODO: add details on CP configuration and when to use it -->
