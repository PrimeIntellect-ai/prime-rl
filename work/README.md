![Alt text](images/parallel_reward.png)

This parallel reward + correctness reward stuff aint workin out boss

Cognition post doesnt even mention a correctness/acc reward, maybe just drop that entirely and only use parallel tool call reward



## Data Setup

VSCode repo, split up into pieces, ground truth extracted, and synthetic user prompt corresponding to ground truth generated.

## Environment Setup

Based on Cognitions SWE-Grep agent blog

Basic sandbox env with tools for grep, glob, read file, list files
