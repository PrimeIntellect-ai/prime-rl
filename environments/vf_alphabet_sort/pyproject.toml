[project]
name = "vf-alphabet-sort"
version = "0.1.0"
dependencies = [
    "verifiers>=0.1.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["vs_alphabet_sort.py"]