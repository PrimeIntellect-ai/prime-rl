[project]
name = "vf-math500-alt"
version = "0.1.0"
dependencies = [
    "verifiers>=0.1.2.post0",
    "math-verify>=0.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["vf_math500_alt.py"]
