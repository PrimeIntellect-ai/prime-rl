# CI eval smoke: reverse-text against the default model on Prime Inference.
num_examples = 16
dashboard = false

[concurrency]
min_inflight = 8
max_inflight = 8

[sampling]
max_completion_tokens = 8192

[[source]]
env.taskset.id = "reverse-text"
env.agent.harness.id = "null"
env.agent.runtime.type = "subprocess"
