# v1 RL ablation: GLM-4.5-Air (100B MoE) on the v1 `r2e-gym-v1` SWE taskset, bash_edit harness on
# a prime sandbox runtime; eval on SWE-Bench Verified. Router replay is off.
#
# 2 train + 2 infer nodes. Trainer uses cp=8 (ulysses) plus
# the memory improvements from the GLM-5.1 prod run (research-prod rlm5/prod.toml): LM-head token
# chunking, activation checkpointing (freq=1) + activation offloading (max_inflight=1), optimizer
# CPU offload, and skip-gather / skip-optimizer checkpointing. Derived from
# configs/debug/v1/glm45air_scaleswe.toml with the training env swapped scaleswe-v1 -> r2e-gym-v1.

max_steps = 400
seq_len = 65536

[slurm]
job_name = "glm45air-r2e"
project_dir = "."
partition = "all"
# Clean up this run's orphaned prime sandboxes before launch (label == SLURM job name).
pre_run_command = 'timeout 120 prime sandbox delete --label "$SLURM_JOB_NAME" -y || true'

[deployment]
type = "multi_node"
num_train_nodes = 2
num_infer_nodes = 1
num_infer_replicas = 2

[wandb]
project = "swe-ablations"
name = "glm45air-r2e"

[weight_broadcast]
type = "nccl"
timeout = 3600

[ckpt]
interval = 50
keep_last = 1
resume_step = -1

[model]
name = "zai-org/GLM-4.5-Air"

# --- Trainer ---

[trainer]
dist_timeout_seconds = 3600

[trainer.model]
impl = "custom"
attn = "flash_attention_3"
cp = 8
cp_style = "ulysses"
fused_lm_head_token_chunk_size = 1024
optim_cpu_offload = true

[trainer.model.ac]

[trainer.model.ac_offloading]
max_inflight_activations = 1

[trainer.model.compile]

[trainer.ckpt]
skip_gather_master_weights = true
skip_optimizer = true

[trainer.optim]
type = "muon"

# --- Orchestrator ---

[orchestrator]
batch_size = 256
group_size = 16
max_off_policy_steps = 32
max_inflight_rollouts = 384

[orchestrator.renderer]
name = "glm-4.5"
preserve_all_thinking = true

[[orchestrator.train.env]]
name = "r2e-gym"
# r2e-gym ships per-task Docker Hub images; resolve them through Prime's registry mirror to avoid
# Docker Hub pull rate limits at this rollout concurrency (set use_prime_registry = false to pull
# straight from Docker Hub). The prime runtime has the GCP pull credentials the mirror needs.
taskset = { id = "r2e-gym-v1", use_prime_registry = true }
harness = { id = "bash_edit", runtime = { type = "prime", labels = ["glm45air-r2e"] } }

[orchestrator.prime_monitor]

# --- Eval: SWE-Bench Verified at step 0 + every 20 steps, bash_edit harness on prime ---

[orchestrator.eval]
interval = 20
skip_first_step = false

[[orchestrator.eval.env]]
name = "swebench-verified"
taskset = { id = "swebench-verified-v1", use_prime_registry = true }
harness = { id = "bash_edit", runtime = { type = "prime", labels = ["glm45air-r2e"] } }
timeout = { rollout = 3600 }

# --- Inference ---

[inference]
gpu_memory_utilization = 0.85
enable_prefix_caching = true

[inference.model]
max_model_len = 65536

[inference.parallel]
tp = 8
