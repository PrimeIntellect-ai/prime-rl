# wiki-search - a search tool served by a shared tool server; the agent runs the
# bash harness with a turn cap.
#
#   uv run eval @ configs/debug/eval/wiki-search.toml

num_examples = 5

[[source]]
env.taskset.id = "wiki-search"
env.agent.max_turns = 10
