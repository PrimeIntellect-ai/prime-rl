# wordle - the textarena taskset pinned to Wordle, the engine playing the user.
#
#   uv run eval @ configs/debug/eval/wordle.toml

num_examples = 5
group_size = 2

[[source]]
env.taskset.id = "wordle"
env.player.max_turns = 8
env.player.harness.id = "null"
