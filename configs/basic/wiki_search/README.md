# wiki_search (2 GPU)

Multi-turn tool calling (search a wiki corpus via MCP tools), LoRA on `Qwen3-4B`. Dev mirror
of [`examples/basic/wiki_search`](../../../examples/basic/wiki_search).

```bash
uv run rl @ configs/basic/wiki_search/rl.toml
```
